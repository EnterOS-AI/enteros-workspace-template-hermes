# Test-time dependencies for the hermes template — NOT baked into the image
# (production deps live in requirements.txt). Install with:
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# or, for a local checkout of both repos, put them on PYTHONPATH:
#
#   PYTHONPATH=../molecule-ai-workspace-runtime:../molecule-ai-sdk pytest tests/

pytest>=8
pytest-asyncio>=0.23
packaging>=23

# ADR-004 §4 adapter-socket conformance suite lives in the SDK
# (molecule_plugin.adapter_conformance); tests/test_conformance.py inherits it.
# Pin the reviewed SDK contract commit so adapter conformance cannot drift when
# SDK main advances. NOT public PyPI; this is a content-addressed Gitea source.
molecule-ai-sdk @ git+https://git.moleculesai.app/molecule-ai/molecule-ai-sdk.git@3474157daca56e3de5b7cffd2a2f84b78bf63b68
